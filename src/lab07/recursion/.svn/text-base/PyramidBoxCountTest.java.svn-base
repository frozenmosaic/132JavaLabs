package lab07.recursion;

import static org.junit.Assert.*;

import org.junit.Test;

public class PyramidBoxCountTest {
	
	private PyramidBoxCount pyr = new PyramidBoxCount();
	
	@Test
	public void testComputeBoxes() {
		assertEquals(1, pyr.computePyramidBoxes(1));
		assertEquals(2, pyr.computePyramidBoxes(2));
		assertEquals(9, pyr.computePyramidBoxes(5));
		assertEquals(12, pyr.computePyramidBoxes(6));
	}

}
